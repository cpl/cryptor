package cryptor

/*
import (
	"github.com/thee-engineer/cryptor/cachedb"
	"github.com/thee-engineer/cryptor/crypt/aes"
)

// APIInterface ...
type APIInterface interface {
	// Cache related operations
	NewCache(string) error
	AddCache(string) error
	DeleteCache(string) error
	ReleaseCache(string) error

	// Infometrics
	CacheSize(string) int
	TotalCacheSize() int
	ChunkCount(string) int
	TotalChunkCount() int

	// Chunk related operations
	ListChunks() []string
	ListChunksInCache(string) []string

	// Chunker operations
	Chunk(string) (string, error)
	ChunkIntoCache(string, string) (string, error)

	// Assembler operations
	Assemble(string, aes.Key)
}

// DefaultAPI ...
type DefaultAPI struct {
	Caches map[string]cachedb.Database
}
*/
